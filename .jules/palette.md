## 2024-04-21 - Format formula names in CLI output
**Learning:** Raw string manipulation is commonly used for terminal output, but `Formatter.identifier` should be used for formula/cask names and commands to ensure consistent styling (green text) and visual hierarchy.
**Action:** Enhance user experience by using `Formatter.identifier` to highlight commands and package names in the CLI output.
